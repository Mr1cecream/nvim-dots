require('telescope').load_extension('howdoi')

-- require('telescope').setup {
    -- extensions = {
        -- howdoi = {
            -- command_executor = { 'python', '-m' },
        -- }
    -- }
-- }
